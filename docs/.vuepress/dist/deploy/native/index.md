---
url: /deploy/native/index.md
---
原生部署配置笔记。

```
Native
├── Package
│   ├── apt install nginx
│   ├── dpkg -i xxx.deb
│   └── yum install xxx
│
├── Binary
│   ├── 下载 sing-box 二进制
│   └── 手动放入 /usr/local/bin
│
└── Source
    └── 源码编译安装
```
